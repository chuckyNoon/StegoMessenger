<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:enabled="true">
        <item
            android:id="@+id/log_out"
            android:enabled="true"
            android:icon="@drawable/ic_24_close"
            android:title="Выйти из аккаунта" />
    </group>

</menu>