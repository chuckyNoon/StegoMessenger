package com.example.stegomessenger.v2.compose.nav

enum class Screens(name: String) {
    LOGIN(name = "login"),
    SIGN_UP(name = "sign_up"),
    OVERVIEW(name = "overview"),
    CHAT(name = "chat"),
    SEARCH(name = "search")
}